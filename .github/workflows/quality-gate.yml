name: BGO Quality Gate

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality-gate:
    runs-on: ubuntu-latest
    env:
      GODOT_VERSION: 4.7.1
      GODOT_STATUS: stable

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install GDScript tooling
        run: pip install "gdtoolkit==4.*"

      - name: Project structure and architecture
        run: python scripts/check_structure.py

      - name: GDScript format
        id: format
        continue-on-error: true
        run: gdformat --check src tests

      - name: GDScript lint
        id: lint
        continue-on-error: true
        run: gdlint src tests

      - name: Report baseline lint debt
        if: steps.format.outcome != 'success' || steps.lint.outcome != 'success'
        run: |
          echo "::warning::Existing GDScript lint/format debt is advisory during the first baseline."
          echo "Format: ${{ steps.format.outcome }}"
          echo "Lint:   ${{ steps.lint.outcome }}"

      - name: Install Godot ${{ env.GODOT_VERSION }}
        shell: bash
        run: |
          set -euo pipefail
          base="https://github.com/godotengine/godot-builds/releases/download/${GODOT_VERSION}-${GODOT_STATUS}"
          curl -fsSL -o /tmp/godot.zip "${base}/Godot_v${GODOT_VERSION}-${GODOT_STATUS}_linux.x86_64.zip"
          unzip -q /tmp/godot.zip -d /tmp/godot
          mkdir -p "$HOME/.local/bin"
          mv "/tmp/godot/Godot_v${GODOT_VERSION}-${GODOT_STATUS}_linux.x86_64" "$HOME/.local/bin/godot"
          chmod +x "$HOME/.local/bin/godot"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Install Web export templates
        shell: bash
        run: |
          set -euo pipefail
          base="https://github.com/godotengine/godot-builds/releases/download/${GODOT_VERSION}-${GODOT_STATUS}"
          curl -fsSL -o /tmp/templates.tpz "${base}/Godot_v${GODOT_VERSION}-${GODOT_STATUS}_export_templates.tpz"
          rm -rf /tmp/godot-templates
          mkdir -p /tmp/godot-templates
          unzip -q /tmp/templates.tpz -d /tmp/godot-templates
          target="$HOME/.local/share/godot/export_templates/${GODOT_VERSION}.${GODOT_STATUS}"
          mkdir -p "$target"
          cp -R /tmp/godot-templates/templates/. "$target/"

      - name: Godot version
        run: godot --version

      - name: Godot import and parse
        run: godot --headless --path . --import --quit

      - name: Headless core tests
        run: godot --headless --path . --script res://tests/test_runner.gd

      - name: Web export smoke test
        run: |
          mkdir -p build/web
          godot --headless --path . --export-release Web build/web/index.html
          node scripts/sync_project_status.mjs
          test -f build/web/index.html
          test -f build/web/project-status/index.html

      - name: Write CI dashboard result
        if: always()
        shell: bash
        run: |
          mkdir -p build/web/project-status
          cat > build/web/project-status/ci.json <<EOF
          {
            "status": "${{ job.status }}",
            "label": "${{ job.status }}",
            "commit": "${{ github.sha }}",
            "branch": "${{ github.ref_name }}",
            "run": "${{ github.run_id }}",
            "format": "${{ steps.format.outcome }}",
            "lint": "${{ steps.lint.outcome }}"
          }
          EOF

      - name: Upload Web smoke-build artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bgo-web-smoke-${{ github.run_id }}
          path: build/web
          if-no-files-found: warn
          retention-days: 7
